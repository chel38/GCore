# {{MODULE_NAME}}

{{MODULE_DESCRIPTION}}

This Module Standard v1 skeleton depends on GCore Core API 1. Add only the client,
NUI, public API, events, and domain behavior the module genuinely needs.

Run `lua tools/module_conformance.lua path/to/{{MODULE_NAME}}` before packaging.
