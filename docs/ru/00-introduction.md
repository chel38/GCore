# Введение / Introduction

## Что такое GreenCore?

GreenCore (GCore) — это минимальный модульный движок для FiveM, написанный **исключительно на Lua 5.4**.

Он отвечает за безопасный жизненный цикл игрока:

```text
Подключение → Проверку → Сессию → Готовность клиента → Спавн → Подтверждение → Отключение
```

## Главный принцип

```text
Сервер является источником истины.
Server is the source of truth.
```

Клиент **не** решает:

- можно ли подключиться;
- где появиться;
- какую модель использовать;
- завершён ли спавн.

Клиент может только:

- сообщить о готовности;
- получить решение сервера;
- выполнить разрешённое действие;
- подтвердить выполнение.

## Технологический стек

| Компонент | Технология |
| --------- | ---------- |
| Сервер    | Lua 5.4    |
| Клиент    | Lua 5.4    |
| Конфигурация | Lua 5.4 |
| Локализация | Lua 5.4 |
| Тесты     | Lua 5.4    |
| Документация | Markdown, Mermaid |
| Module NUI | TypeScript + Tailwind CSS |
| Identity persistence | MariaDB + oxmysql |

> **`gc_identity` уже использует TypeScript + Tailwind CSS для NUI. C# не используется.**

## Что НЕ входит в первую версию

- Регистрация, аккаунты и персонажи внутри `gc_core` (ими владеет `gc_identity`)
- Общая database/ORM внутри `gc_core`
- Деньги, инвентарь, транспорт
- Чат, HUD, админ-панель

## Структура проекта

```text
GreenCore/
├── README.md
├── docs/
│   ├── ru/
│   ├── en/
│   └── diagrams/
├── examples/
└── resources/
    └── [greencore]/
        ├── gc_core/
        ├── gc_example/
        └── gc_identity/
```

## Следующий шаг

Перейдите к [Установке](01-installation.md).
