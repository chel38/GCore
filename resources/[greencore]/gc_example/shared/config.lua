-- EN: The reference module depends on the API contract, not a core patch.
-- RU: Reference module зависит от API-контракта, а не от patch-версии core.

GCExampleConfig = {
    requiredCoreApi = 1,
    command = 'gcexample'
}
